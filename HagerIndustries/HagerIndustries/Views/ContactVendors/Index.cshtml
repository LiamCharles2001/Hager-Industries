@model IEnumerable<HagerIndustries.Models.ContactVendor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vendor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vendor.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Contact.Email)
            </td>
            <td>
                @if (User.IsInRole("Admin") || User.IsInRole("Supervisor"))
                {
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) <span> | </span>
                }
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) <span> | </span>
                @if (User.IsInRole("Admin") || User.IsInRole("Supervisor"))
                {
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                }
            </td>
        </tr>
}
    </tbody>
</table>
